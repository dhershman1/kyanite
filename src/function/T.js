
/**
 * @name T
 * @function
 * @since v1.2.0
 * @category Function
 * @sig () -> Boolean
 * @description
 * A Function that will always return true, any given parameters are ignored
 * @return {Boolean} A boolean of true
 * @example
 * import { T } from 'kyanite'
 *
 * T() // => true
 */
const T = () => true

export default T
