language: node_js
node_js:
  - "8"
install:
  - npm install -g codecov
branches:
  only:
  - master
  - development
script:
  - npm run lint
  - npm run test:cov
  - npm run check-cov
after_success: npm run report
