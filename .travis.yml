language: node_js
node_js:
  - "10"
install:
  - npm ci
cache:
  directories:
  - "$HOME/.npm"
branches:
  only:
  - master
  - development
script:
  - npm run lint
  - npm run test:cov
  - npm run check-cov
after_success: npm run report
